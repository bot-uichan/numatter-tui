{
  "name": "numatter-tui-monorepo",
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter numatter-tui dev",
    "start": "pnpm --filter numatter-tui start",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.8"
  }
}
